#include "main.h"
#include <stdio.h>
/**
 * _isupper - function that checks uppercase characters
 * @m: characters entered
 *
 * Return: 1 if character is uppercase otherwise 0
 */
int _isupper(int m)
{
	if (m >= 65 && m <= 90)
	{
	return (1);
	}
	return (0);
}
