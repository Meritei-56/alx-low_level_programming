#include "main.h"
#include "stdio.h"
/**
 * mul: finds the product of two integers
 * @m: integer
 * @n: integer
 * Return: multiplication of two integers
 */
int mul(int m, int n)
{
	int mult;

	mult = m * n;

	return (mult);
}
